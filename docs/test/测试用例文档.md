# 考勤异常检测与预警系统测试用例文档

## 1. 文档说明

### 1.1 编写目的
本文档用于描述考勤异常检测与预警系统的测试目标、测试范围、测试环境、测试策略及核心测试用例，为系统联调、验收和毕业设计测试章节撰写提供依据。

### 1.2 测试目标
- 验证系统各功能模块运行正确
- 验证基础考勤异常判定逻辑准确性
- 验证复杂考勤异常识别流程可用性
- 验证预警、人工复核和报表功能完整性
- 验证系统性能、安全性和可用性满足设计要求

## 2. 测试环境

### 2.1 硬件环境
- 普通 Windows 办公电脑
- 摄像头设备
- 本地网络环境

### 2.2 软件环境
- 操作系统：Windows 10 或以上
- JDK：1.8 及以上
- 前端：Vue3 + Element Plus
- 后端：SpringBoot
- 数据库：MySQL 8.x，使用 `system` 库
- 缓存：Redis 6.x
- 浏览器：Chrome
- 接口测试工具：Postman

测试相关核心表：
- `user`、`department`、`role`
- `device`、`faceFeature`、`rule`
- `attendanceRecord`、`attendanceException`
- `exceptionAnalysis`、`warningRecord`、`reviewRecord`、`operationLog`

测试数据建议通过 `D:\Graduation project\backend\sql\seed\测试数据插入SQL.sql` 导入。

## 3. 测试范围
- 用户与权限管理
- 人脸采集与打卡
- 基础异常判定
- 复杂异常智能分析
- 预警与人工复核
- 统计分析与报表
- 提示词模板与智能决策支撑
- 安全性与性能

## 4. 功能测试用例

| 用例编号 | 模块 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC001 | 登录认证 | 管理员登录成功 | 已存在管理员账号 | 输入正确账号密码登录 | 登录成功，进入后台首页 |
| TC002 | 登录认证 | 用户登录失败 | 已存在账号 | 输入错误密码登录 | 提示账号或密码错误 |
| TC003 | 用户管理 | 新增员工成功 | 管理员已登录 | 填写员工信息并提交 | 员工新增成功，列表可查询 |
| TC004 | 人脸管理 | 人脸录入成功 | 员工账号已创建 | 上传或采集人脸图像 | 人脸特征保存成功 |
| TC005 | 考勤打卡 | 正常上班打卡 | 员工已录入人脸 | 在正常时间段完成打卡 | 打卡成功，状态为正常 |
| TC006 | 基础异常 | 迟到判定 | 已配置上班时间 | 员工超过阈值后打卡 | 系统判定为迟到 |
| TC007 | 基础异常 | 早退判定 | 已配置下班时间 | 员工早于阈值离开打卡 | 系统判定为早退 |
| TC008 | 基础异常 | 重复打卡判定 | 员工已完成一次打卡 | 短时间内再次打卡 | 系统判定为重复打卡 |
| TC009 | 基础异常 | 缺勤判定 | 已到结算时间 | 员工当天无打卡记录 | 系统生成缺勤记录 |
| TC010 | 复杂异常 | 疑似代打卡识别 | 已存在员工历史记录 | 使用异常设备或临界人脸分数打卡 | 系统触发复杂异常分析并输出疑似代打卡 |
| TC011 | 复杂异常 | 多设备异常识别 | 员工近期有设备历史 | 短时间内切换不同设备打卡 | 系统识别为多设备异常风险 |
| TC012 | 复杂异常 | 多地点异常识别 | 已记录地点信息 | 短时间内在不合理地点跨度打卡 | 系统识别为多地点异常 |
| TC013 | 复杂异常 | 行为组合异常识别 | 存在多条历史记录 | 连续多日在临界时间打卡并伴随补卡 | 系统触发组合行为异常分析 |
| TC014 | 预警管理 | 高风险异常预警生成 | 存在高风险异常结果 | 系统完成异常判定 | 生成预警记录并在前端展示 |
| TC015 | 人工复核 | 管理员确认异常 | 存在待复核异常 | 管理员查看详情并确认 | 复核结果保存，异常状态更新 |
| TC016 | 报表统计 | 个人考勤统计查询 | 已存在多条考勤记录 | 查询指定员工统计报表 | 正确展示出勤率和异常率 |
| TC017 | 报表导出 | 报表导出成功 | 存在统计结果 | 点击导出报表 | 成功导出文件 |
| TC018 | 登录认证 | 禁用角色登录失败 | 已存在绑定禁用角色的账号 | 输入正确账号密码登录 | 提示账号角色已禁用 |
| TC019 | 登录认证 | 空用户名校验 | 登录接口可访问 | 用户名置空后提交登录 | 提示用户名不能为空 |
| TC020 | 登录认证 | 空密码校验 | 登录接口可访问 | 密码置空后提交登录 | 提示密码不能为空 |
| TC021 | 部门管理 | 查询部门列表成功 | 管理员已登录，存在部门数据 | 访问部门列表接口 | 返回部门列表数据 |
| TC022 | 部门管理 | 新增部门成功 | 管理员已登录 | 提交合法部门名称与描述 | 部门新增成功 |
| TC023 | 部门管理 | 修改部门成功 | 管理员已登录，目标部门存在 | 提交合法修改信息 | 部门修改成功 |
| TC024 | 部门管理 | 删除无引用部门成功 | 管理员已登录，目标部门无用户引用 | 删除目标部门 | 删除成功 |
| TC025 | 部门管理 | 删除被引用部门失败 | 管理员已登录，目标部门存在关联用户 | 删除目标部门 | 提示部门下存在关联用户，不能删除 |
| TC026 | 用户管理 | 查询员工列表成功 | 管理员已登录，存在员工数据 | 访问员工列表接口 | 返回员工列表数据 |
| TC027 | 用户管理 | 新增员工失败（用户名重复） | 管理员已登录，目标账号已存在 | 提交重复用户名 | 提示用户名已存在 |
| TC028 | 用户管理 | 修改员工成功 | 管理员已登录，目标员工存在 | 提交合法修改信息 | 员工信息更新成功 |
| TC029 | 用户管理 | 修改密码后登录成功 | 管理员已登录，目标员工存在 | 更新员工密码后使用新密码登录 | 登录成功 |
| TC030 | 用户管理 | 删除员工成功 | 管理员已登录，目标员工存在 | 删除目标员工 | 删除成功 |
| TC031 | 部门管理 | 新增并维护部门信息 | 管理员已登录 | 新增部门后修改部门名称或描述并提交 | 部门新增成功，修改内容在列表中正确显示 |
| TC032 | 角色管理 | 新增并维护角色信息 | 管理员已登录 | 新增角色后修改角色名称、描述或状态并提交 | 角色新增成功，修改结果在列表中正确显示 |
| TC033 | 设备管理 | 新增设备并切换状态 | 管理员已登录 | 新增设备后执行启用或停用状态切换 | 设备新增成功，状态切换后列表展示正确 |
| TC034 | 考勤记录查询 | 查询个人考勤记录 | 员工已存在历史打卡记录 | 按员工编号查询个人考勤记录列表 | 返回该员工的打卡记录明细 |
| TC035 | 补卡申请 | 提交补卡申请成功 | 员工账号已登录且存在缺失打卡场景 | 填写补卡时间和补卡原因并提交申请 | 补卡申请提交成功，系统生成待处理记录 |
| TC036 | 系统配置 | 修改考勤规则后生效 | 已存在启用中的考勤规则 | 修改迟到阈值等规则配置后保存，并按新规则执行打卡验证 | 配置保存成功，后续判定按最新规则生效 |
| TC037 | 系统配置 | 维护风险等级配置 | 管理员已登录且存在风险等级配置 | 修改风险等级名称、描述或状态后保存 | 风险等级配置更新成功，前端展示与预警分级保持一致 |
| TC038 | 系统配置 | 维护异常类型配置 | 管理员已登录且存在异常类型配置 | 修改异常类型名称、描述或状态后保存 | 异常类型配置更新成功，复核与展示页面可使用最新类型 |
| TC039 | 系统审计 | 执行关键操作后查询日志 | 管理员已登录且系统开启日志审计 | 先执行部门新增或规则修改等关键操作，再查询操作日志列表 | 日志列表中可查询到新增的审计记录，包含操作类型、内容与时间 |
| TC040 | 智能异常分析 | 查询异常决策链 | 已存在复杂异常分析结果 | 查看异常详情并查询决策链 | 可展示规则结果、模型结果、最终结论和置信度 |
| TC041 | 智能异常分析 | 查询异常分析摘要 | 已存在复杂异常分析结果 | 查询异常分析摘要接口 | 返回模型结论、理由摘要、处理建议和相似案例摘要 |
| TC042 | 智能预警 | 查询预警建议 | 已存在高风险预警记录 | 查询预警建议接口 | 返回 AI 预警摘要、优先级和处置建议 |
| TC043 | 人工复核 | 查询 AI 复核辅助信息 | 已存在待复核异常 | 查询复核辅助接口 | 返回 AI 复核建议、相似案例摘要和决策依据 |
| TC044 | 人工复核 | 提交复核反馈标签 | 已完成一次复核 | 提交误报或建议采纳等反馈标签 | 系统保存反馈标签和策略反馈说明 |
| TC045 | 统计分析 | 生成统计摘要 | 已存在异常与复核历史数据 | 查询统计摘要接口 | 返回周期摘要、重点风险和管理建议 |
| TC046 | 统计分析 | 生成部门风险画像 | 已存在部门级统计数据 | 查询部门风险画像摘要接口 | 返回风险评分、风险摘要和建议 |
| TC047 | 系统配置 | 管理提示词模板 | 管理员已登录且系统已存在模板数据 | 新增或更新提示词模板后保存 | 提示词模板保存成功，可按场景和状态查询 |

## 5. 输入数据示例

在执行以下测试前，建议先导入 `数据库建表SQL.sql` 和 `测试数据插入SQL.sql`，确保 `user`、`attendanceRecord`、`attendanceException` 等核心表存在基础样例数据，并可结合打卡记录中的设备编号进行验证。

### 5.1 基础测试数据
注：表中“正常上班时间”“正常下班时间”为测试规则假设或补充配置，用于说明测试判定窗口，不是当前种子 SQL 的直接存储字段。
| 测试用户ID | 姓名 | 部门 | 角色 | 常用设备 | 常用地点 | 正常上班时间 | 正常下班时间 |
|---|---|---|---|---|---|---|---|
| 1001 | 张三 | 技术部 | 员工 | DEV-001 | 办公区A | 08:55-09:05 | 17:55-18:10 |
| 1002 | 李四 | 行政部 | 员工 | DEV-002 | 办公区B | 08:50-09:00 | 17:50-18:00 |

### 5.2 异常场景输入示例
以下示例中，示例一至示例三可在导入当前种子 SQL 后直接复现；示例四可结合现有原始记录验证，若需独立验证也可补充构造。
- 示例一：迟到。员工 `1002` 于 `09:16` 使用 `DEV-002` 打卡，预期判定为迟到。
- 示例二：重复打卡。员工 `1001` 在 `08:58` 和 `09:00` 通过 `DEV-001` 连续打卡，预期判定为重复打卡。
- 示例三：疑似代打卡。员工 `1002` 于 `08:58:30` 使用异常设备 `DEV-009` 打卡，人脸相似度 `82.4`，预期触发复杂异常分析。
- 示例四：多设备异常。当前种子 SQL 已包含用户 `1002` 的跨设备原始记录，但未单独提供多设备异常结果；如需独立验证，仍可为同一员工补充短时间内跨设备打卡记录后再验证多设备异常分析。

## 6. 接口测试用例

| 用例编号 | 接口 | 测试内容 | 输入 | 预期结果 |
|---|---|---|---|---|
| API001 | `POST /api/auth/login` | 正常登录 | 正确账号密码 | 返回 token 和角色信息 |
| API002 | `POST /api/auth/login` | 异常登录 | 错误账号密码 | 返回登录失败信息 |
| API003 | `POST /api/attendance/checkin` | 正常打卡 | 合法打卡参数 | 返回打卡成功 |
| API004 | `POST /api/exception/complex-check` | 复杂异常分析 | 合法风险特征参数 | 返回异常类型、风险等级、判定依据 |
| API005 | `POST /api/review/submit` | 提交复核结果 | 合法复核参数 | 返回提交成功 |
| API006 | `POST /api/auth/login` | 参数校验 | 空用户名或空密码 | 返回参数校验失败信息 |
| API007 | `GET /api/user/list` | token 无效或过期 | 无效 token / 过期 token | 返回未授权信息 |
| API008 | `GET /api/department/list` | 查询部门列表 | 可选关键字参数 | 返回部门列表 |
| API009 | `POST /api/department/add` | 新增部门 | 合法部门参数 | 返回新增部门信息 |
| API010 | `PUT /api/department/update` | 修改部门 | 合法部门参数 | 返回更新后的部门信息 |
| API011 | `DELETE /api/department/{id}` | 删除部门 | 无引用的部门ID | 返回删除成功 |
| API012 | `GET /api/user/list` | 查询员工列表 | 可选关键字、部门、状态参数 | 返回员工列表 |
| API013 | `POST /api/user/add` | 新增员工 | 合法员工参数 | 返回新增员工信息 |
| API013A | `POST /api/user/add` | 用户新增接口畸形 JSON 校验 | 畸形 JSON 请求体 | 返回请求参数错误 |
| API014 | `PUT /api/user/update` | 修改员工 | 合法员工参数 | 返回更新后的员工信息 |
| API014A | `PUT /api/user/update` | 用户修改接口畸形 JSON 校验 | 畸形 JSON 请求体 | 返回请求参数错误 |
| API015 | `DELETE /api/user/{id}` | 删除员工 | 合法员工ID | 返回删除成功 |
| API016 | `GET /api/device/list` | 查询设备列表 | 管理员 token | 返回设备列表，字段使用 `deviceId` |
| API017 | `GET /api/device/list` | 关键字与状态筛选设备 | `keyword=办公区`、`status=1` | 返回符合条件的设备列表 |
| API018 | `POST /api/device/add` | 新增设备 | 合法设备参数 | 返回新增后的设备信息 |
| API019 | `POST /api/device/add` | 设备新增编号必填校验 | `deviceId` 为空白 | 返回 `设备编号不能为空` |
| API020 | `POST /api/device/add` | 设备新增名称必填校验 | `name` 为空白 | 返回 `设备名称不能为空` |
| API020A | `POST /api/device/add` | 设备新增状态合法性校验 | `status=2` | 返回 `设备状态不合法` |
| API021 | `POST /api/device/add` | 设备新增编号唯一校验 | 重复 `deviceId` | 返回 `设备编号已存在` |
| API022 | `POST /api/device/add` | 设备新增接口畸形 JSON 校验 | 畸形 JSON 请求体 | 返回请求参数错误 |
| API023 | `PUT /api/device/update` | 修改设备 | 合法设备参数 | 返回更新后的设备信息，`deviceId` 不变 |
| API024 | `PUT /api/device/update` | 设备修改必填校验 | `deviceId` 或 `name` 为空白 | 返回对应参数错误信息 |
| API024A | `PUT /api/device/update` | 设备修改状态合法性校验 | `status=2` | 返回 `设备状态不合法` |
| API025 | `PUT /api/device/update` | 设备修改接口畸形 JSON 校验 | 畸形 JSON 请求体 | 返回请求参数错误 |
| API026 | `PUT /api/device/status` | 修改设备状态 | 合法 `deviceId` 和 `status` | 返回更新后的设备状态 |
| API027 | `PUT /api/device/status` | 设备状态非法校验 | `status=2` 或缺失编号 | 返回对应参数错误信息 |
| API027A | `PUT /api/device/status` | 设备状态接口畸形 JSON 校验 | 畸形 JSON 请求体 | 返回请求参数错误 |
| API028 | `DELETE /api/device/{deviceId}` | 删除未引用设备 | 未被打卡记录引用的设备编号 | 返回删除成功 |
| API029 | `DELETE /api/device/{deviceId}` | 删除不存在设备 | 不存在的设备编号 | 返回 `设备不存在` |
| API030 | `DELETE /api/device/{deviceId}` | 删除已引用设备 | 已被打卡记录引用的设备编号 | 返回 `设备已关联打卡记录，不能删除` |
| API031 | `GET /api/role/list` | 查询角色列表 | 合法查询参数 | 返回角色列表数据 |
| API032 | `GET /api/attendance/record/{userId}` | 查询个人考勤记录 | 合法用户编号 | 返回个人考勤记录明细 |
| API033 | `GET /api/attendance/list` | 查询考勤记录列表 | 合法查询参数 | 返回考勤记录列表数据 |
| API034 | `POST /api/attendance/repair` | 提交补卡申请 | 合法补卡参数 | 返回提交成功 |
| API035 | `PUT /api/rule/update` | 更新考勤规则 | 合法规则参数 | 返回更新成功 |
| API036 | `GET /api/system/risk-level/list` | 查询风险等级配置 | 合法查询参数 | 返回风险等级配置列表 |
| API037 | `PUT /api/system/risk-level/update` | 更新风险等级配置 | 合法风险等级参数 | 返回更新成功 |
| API038 | `GET /api/system/exception-type/list` | 查询异常类型配置 | 合法查询参数 | 返回异常类型配置列表 |
| API039 | `PUT /api/system/exception-type/update` | 更新异常类型配置 | 合法异常类型参数 | 返回更新成功 |
| API040 | `GET /api/log/operation/list` | 查询操作日志 | 合法查询参数 | 返回操作日志列表数据 |
| API041 | `DELETE /api/role/{id}` | 删除角色 | 合法角色编号 | 返回删除成功 |
| API042 | `GET /api/rule/list` | 查询考勤规则列表 | 合法查询参数 | 返回考勤规则列表数据 |
| API043 | `POST /api/rule/add` | 新增考勤规则 | 合法规则参数 | 返回新增成功 |
| API044 | `PUT /api/rule/status` | 更新考勤规则状态 | 合法状态参数 | 返回更新成功 |
| API045 | `GET /api/exception/{id}/decision-trace` | 查询异常决策链 | 合法异常编号 | 返回规则结果、模型结果和最终结论 |
| API046 | `GET /api/exception/{id}/analysis-brief` | 查询异常分析摘要 | 合法异常编号 | 返回模型结论、理由摘要和处理建议 |
| API047 | `GET /api/warning/{id}/advice` | 查询预警建议 | 合法预警编号 | 返回 AI 预警摘要、优先级和处置建议 |
| API048 | `POST /api/warning/re-evaluate` | 重新评估预警优先级 | 合法预警参数 | 返回重评估成功 |
| API049 | `GET /api/review/{exceptionId}/assistant` | 查询 AI 复核辅助信息 | 合法异常编号 | 返回 AI 复核建议与相似案例摘要 |
| API050 | `POST /api/review/feedback` | 提交复核反馈标签 | 合法反馈参数 | 返回提交成功 |
| API051 | `GET /api/statistics/summary` | 查询统计摘要 | 合法查询参数 | 返回周期摘要和管理建议 |
| API052 | `GET /api/statistics/department-risk-brief` | 查询部门风险画像摘要 | 合法部门参数 | 返回风险评分和摘要 |
| API053 | `GET /api/system/prompt/list` | 查询提示词模板列表 | 合法查询参数 | 返回提示词模板列表 |
| API054 | `POST /api/system/prompt/add` | 新增提示词模板 | 合法模板参数 | 返回新增成功 |
| API055 | `PUT /api/system/prompt/update` | 更新提示词模板 | 合法模板参数 | 返回更新成功 |
| API056 | `PUT /api/system/prompt/status` | 更新提示词模板状态 | 合法状态参数 | 返回更新成功 |
| API057 | `GET /api/system/model-log/list` | 查询模型调用日志 | 合法查询参数 | 返回模型调用日志列表 |
| API058 | `POST /api/face/register` | 人脸录入成功 | 管理员 token + 合法 `userId`、`imageData` | 返回录入成功结果，不暴露原始特征 |
| API059 | `POST /api/face/register` | 人脸录入用户编号必填校验 | `userId` 缺失 | 返回 `用户编号不能为空` |
| API060 | `POST /api/face/register` | 人脸录入图像必填校验 | `imageData` 为空白 | 返回 `人脸图像不能为空` |
| API061 | `POST /api/face/register` | 人脸录入用户存在性校验 | 不存在的 `userId` | 返回 `用户不存在` |
| API062 | `POST /api/face/register` | 人脸录入接口畸形 JSON 校验 | 畸形 JSON 请求体 | 返回 `请求参数错误` |
| API063 | `POST /api/face/verify` | 未录入用户的人脸验证 | 合法 `userId` + 合法 `imageData` | 返回 `registered=false`、`matched=false` |
| API064 | `POST /api/face/verify` | 已录入用户验证通过 | 已录入模板 + 相同 `imageData` | 返回 `registered=true`、`matched=true` 和 `faceScore` |
| API065 | `POST /api/face/verify` | 已录入用户验证失败 | 已录入模板 + 不同 `imageData` | 返回 `registered=true`、`matched=false` |
| API066 | `POST /api/face/verify` | 人脸验证接口畸形 JSON 校验 | 畸形 JSON 请求体 | 返回 `请求参数错误` |

## 7. 性能测试用例

| 用例编号 | 测试项 | 测试条件 | 预期结果 |
|---|---|---|---|
| PT001 | 并发访问测试 | 30 人同时访问系统 | 系统运行稳定，无明显卡顿 |
| PT002 | 打卡响应测试 | 正常网络环境下提交打卡 | 响应时间不超过 3 秒 |
| PT003 | 异常分析响应测试 | 触发复杂异常分析 | 分析结果在合理时间内返回 |
| PT004 | 模型调用性能测试 | 同时触发复杂异常分析和预警摘要生成 | 模型调用延迟在可接受范围内，系统可正常降级 |

## 8. 安全测试用例

| 用例编号 | 测试项 | 测试步骤 | 预期结果 |
|---|---|---|---|
| ST001 | 未登录访问拦截 | 未登录直接访问后台接口 | 系统返回未授权 |
| ST002 | 越权访问测试 | 员工账号访问管理员接口 | 系统拒绝访问 |
| ST003 | 敏感数据保护 | 查询人脸特征等敏感数据 | 非授权用户不可访问 |
| ST004 | 日志审计测试 | 执行关键操作 | 系统记录操作日志 |
| ST005 | 无效 token 访问拦截 | 携带无效 token 访问管理接口 | 系统返回未授权 |
| ST006 | 过期 token 访问拦截 | 携带过期 token 访问管理接口 | 系统返回未授权 |
| ST007 | 默认受保护路径拦截 | 未登录访问未显式放行的 `/api/**` 路径 | 系统返回未授权 |
| ST008 | 员工访问其他接口命名空间拦截 | 员工 token 访问非登录接口的其他 `/api/**` 路径 | 系统返回禁止访问 |
| ST009 | 畸形会话拦截 | token 对应会话缺少必要字段 | 系统返回未授权 |
| ST010 | 部门接口越权访问拦截 | 员工 token 访问 `/api/department/list` | 系统返回禁止访问 |
| ST011 | 模型输入脱敏测试 | 触发模型分析并检查调用日志 | 日志中不直接保存敏感原始数据 |
| ST012 | 模型管理接口权限测试 | 员工访问提示词模板或模型日志接口 | 系统拒绝访问 |

## 9. 实际测试结果记录表

### 9.1 功能测试记录模板
| 测试编号 | 测试日期 | 测试人员 | 用例名称 | 输入数据 | 预期结果 | 实际结果 | 是否通过 | 备注 |
|---|---|---|---|---|---|---|---|---|
| TC001 | 2026-03-26 | xxx | 管理员登录成功 | admin/123456 | 登录成功 | 登录成功 | 通过 | - |
| TC006 | 2026-03-26 | xxx | 迟到判定 | 09:16打卡 | 判定迟到 | 判定迟到 | 通过 | - |
| TC010 | 2026-03-26 | xxx | 疑似代打卡识别 | DEV-009 + 82.4分 | 高风险异常 | 高风险异常 | 通过 | - |

### 9.2 性能测试记录模板
| 测试编号 | 测试场景 | 并发量 | 平均响应时间 | 最大响应时间 | CPU占用 | 内存占用 | 是否通过 |
|---|---|---|---|---|---|---|---|
| PT001 | 并发访问测试 | 30 | 1.8s | 2.7s | 63% | 58% | 通过 |
| PT002 | 打卡响应测试 | 10 | 1.2s | 1.9s | 41% | 46% | 通过 |

## 10. 测试结论模板
系统经功能测试、接口测试、性能测试和安全测试后，核心业务流程运行正常。基础异常判定逻辑能够正确识别迟到、早退、缺勤和重复打卡等情况；复杂异常检测模块能够对代打卡、多设备异常打卡、多地点异常打卡和行为组合异常进行有效分析；AI 判定依据、AI 预警建议、AI 复核辅助和 AI 统计摘要能够正确生成；预警与人工复核流程完整，报表统计结果正确，模型调用日志和决策追踪链路可审计，系统整体满足课题设计目标。
